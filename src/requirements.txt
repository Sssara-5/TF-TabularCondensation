# Prefer conda: environment.yml (PyTorch CUDA + faiss-gpu on one stack).
#
# Pip: install a CUDA build of PyTorch from https://pytorch.org if you use GPU.
# CCTC needs faiss with GPU (conda faiss-gpu, or a matching faiss-gpu pip wheel).
# Then:
#   pip install -r requirements.txt
torch>=2.0
click>=8.0
joblib>=1.3
matplotlib>=3.7
numpy>=1.24
openml>=0.14
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
skrub>=0.4.0
tqdm>=4.65
